---
import Layout from '../layouts/Layout.astro';
import expData from '../data/experience.json';

const defaultLang = 'es';

const expSchema = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  "name": "Experiencia Laboral — Pablo Gómez",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1,
      "name": "Programador Full Stack — EDUCA EDTECH Group",
      "description": "Desarrollo y mantenimiento de plataformas educativas en producción. Oct. 2023 – Jun. 2024"
    },
    {
      "@type": "ListItem",
      "position": 2,
      "name": "Junior Software Developer — Euroinnova",
      "description": "Desarrollo de aplicaciones web en el sector de la formación online. Abr. 2023 – Oct. 2023"
    }
  ]
};
---

<Layout
  title="Experiencia — Pablo Gómez | Full Stack Developer"
  showHero={false}
  description="Trayectoria profesional de Pablo Gómez: más de 1 año de experiencia como Full Stack Developer en EDUCA EDTECH Group y Euroinnova. Laravel, PHP, MySQL, Google Cloud."
  canonical="https://www.pablogomezvillen.com/experiencia"
  alternateEs="https://www.pablogomezvillen.com/experiencia"
  alternateEn="https://www.pablogomezvillen.com/en/experiencia"
  keywords="experiencia laboral, Full Stack Developer, Laravel, PHP, EDUCA EDTECH, Euroinnova, Granada, desarrollador backend"
  jsonLd={expSchema}
>

  <!-- Banner -->
  <div class="page-banner">
    <h1 data-i18n="section-experiencia.page-title">
      <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="2" y="7" width="20" height="14" rx="2" ry="2"/><path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/></svg>
      Experiencia
    </h1>
    <p data-i18n="section-experiencia.subtitle">Mi trayectoria profesional como desarrollador</p>
    <p class="banner-rescue">¿Buscas una web para tu negocio? <a href="/servicios">Ver servicios →</a></p>
    <a href="/CV_Full-stack_Pablo_Gomez_Villen_Desarrollador.pdf" download target="_blank" rel="noopener noreferrer" class="btn secondary btn--icon page-banner-cv">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
      Descargar CV
    </a>
  </div>

  <section class="section">

    <!-- Stats rápidas -->
    <div class="exp-stats">
      <div class="exp-stat">
        <span class="exp-stat-num">1+</span>
        <span class="exp-stat-label" data-i18n="section-experiencia.stats.years">año de experiencia</span>
      </div>
      <div class="exp-stat">
        <span class="exp-stat-num">2</span>
        <span class="exp-stat-label" data-i18n="section-experiencia.stats.companies">empresas</span>
      </div>
      <div class="exp-stat">
        <span class="exp-stat-num">2</span>
        <span class="exp-stat-label" data-i18n="section-experiencia.stats.projects">proyectos en producción</span>
      </div>
    </div>

    <!-- Timeline -->
    <div class="exp-timeline">
      {expData.experiencia.map((job) => (
        <div class="exp-item" id={`exp-${job.id}`} data-exp-id={String(job.id)}>
          <div class="exp-dot"></div>
          <div class="exp-card">
            <div class="exp-header">
              <div class="exp-header-left">
                <h2 class="exp-role">{job.role[defaultLang as 'es' | 'en']}</h2>
                <p class="exp-company">{job.company}</p>
              </div>
              <div class="exp-badges">
                <span class="exp-badge exp-duration">{job.duration[defaultLang as 'es' | 'en']}</span>
                <span class="exp-badge exp-badge--mode exp-modality">{job.modality[defaultLang as 'es' | 'en']}</span>
              </div>
            </div>

            <p class="exp-period">
              <span class="exp-period-text">{job.period[defaultLang as 'es' | 'en']}</span>
              {' · '}{job.location}
            </p>

            <p class="exp-desc">{job.description[defaultLang as 'es' | 'en']}</p>

            <ul class="exp-bullets">
              {job.bullets[defaultLang as 'es' | 'en'].map((b) => (
                <li>{b}</li>
              ))}
            </ul>

            <div class="skills">
              {job.skills.map(skill => (
                <span class={`skill ${skill.level}`}>{skill.name}</span>
              ))}
            </div>
          </div>
        </div>
      ))}
    </div>

  </section>

  <script type="module">
    import { loadLanguage, getLanguage } from '/scripts/i18n.js';

    window.addEventListener('DOMContentLoaded', async () => {
      const lang = getLanguage();
      await loadLanguage(lang);
      const idiomaActual = window.lang || lang;

      if (idiomaActual === 'es') return;

      const data = await fetch('/data/experience.json').then(r => r.json());

      data.experiencia.forEach(job => {
        const card = document.querySelector(`[data-exp-id="${job.id}"]`);
        if (!card) return;

        const get = sel => card.querySelector(sel);

        const roleEl = get('.exp-role');
        if (roleEl && job.role[idiomaActual]) roleEl.textContent = job.role[idiomaActual];

        const periodEl = get('.exp-period-text');
        if (periodEl && job.period[idiomaActual]) periodEl.textContent = job.period[idiomaActual];

        const durationEl = get('.exp-duration');
        if (durationEl && job.duration[idiomaActual]) durationEl.textContent = job.duration[idiomaActual];

        const modalityEl = get('.exp-modality');
        if (modalityEl && job.modality[idiomaActual]) modalityEl.textContent = job.modality[idiomaActual];

        const descEl = get('.exp-desc');
        if (descEl && job.description[idiomaActual]) descEl.textContent = job.description[idiomaActual];

        const bulletsEl = get('.exp-bullets');
        if (bulletsEl && job.bullets[idiomaActual]) {
          bulletsEl.innerHTML = job.bullets[idiomaActual].map(b => `<li>${b}</li>`).join('');
        }
      });
    });
  </script>
</Layout>
