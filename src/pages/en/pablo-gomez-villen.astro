---
import Layout from '../../layouts/Layout.astro';
import enData from '../../data/i18n/en.json';

const sm = enData['sobre-mi'];
---

<Layout
  title="Pablo Gómez Villén | Full Stack Developer in Granada"
  lang="en"
  showHero={false}
  description="Meet Pablo Gómez, 23-year-old Full Stack Developer in Granada. Specializing in Laravel, PHP and JavaScript, with training in AI and Big Data."
  canonical="https://www.pablogomezvillen.com/en/pablo-gomez-villen"
  alternateEs="https://www.pablogomezvillen.com/pablo-gomez-villen"
  alternateEn="https://www.pablogomezvillen.com/en/pablo-gomez-villen"
  keywords="about me, Pablo Gómez, developer Granada, Full Stack Laravel PHP, AI Big Data, DAW, programmer Spain"
>

  <canvas id="sm-canvas"></canvas>

  <div class="sm-page">

    <!-- Hero -->
    <section class="sm-hero">
      <div class="sm-hero-inner">
        <picture>
          <source type="image/webp" srcset="/img/foto-empresa.webp">
          <img src="/img/foto-empresa.jpg" alt="Pablo Gómez, Full Stack Developer in Granada" class="sm-photo ani" style="--d:0s">
        </picture>
        <span class="sm-chip ani" style="--d:.05s">{sm.chip}</span>
        <h1 class="sm-name ani" style="--d:.1s">Pablo <span>Gómez Villén</span></h1>
        <p class="sm-role ani" style="--d:.2s">{sm.role}</p>
        <p class="sm-intro ani" style="--d:.3s">{sm.intro}</p>
      </div>
      <a href="#stats" class="sm-scroll-hint">
        <span>{sm['scroll-cta']}</span>
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
      </a>
    </section>

    <!-- Stats -->
    <div class="sm-stats" id="stats">
      <div class="sm-stat reveal">
        <div class="sm-stat-val"><span class="counter" data-target="1">1</span><span>+</span></div>
        <div class="sm-stat-lbl">{sm.stats.years}</div>
      </div>
      <div class="sm-stat reveal" style="--d:.1s">
        <div class="sm-stat-val"><span class="counter" data-target="2">2</span></div>
        <div class="sm-stat-lbl">{sm.stats.companies}</div>
      </div>
      <div class="sm-stat reveal" style="--d:.2s">
        <div class="sm-stat-val"><span class="counter" data-target="3">3</span></div>
        <div class="sm-stat-lbl">{sm.stats.formations}</div>
      </div>
      <div class="sm-stat reveal" style="--d:.3s">
        <div class="sm-stat-val"><span class="counter" data-target="10">10</span><span>+</span></div>
        <div class="sm-stat-lbl">{sm.stats.techs}</div>
      </div>
    </div>

    <!-- Timeline -->
    <section class="sm-section">
      <h2 class="sm-title reveal" set:html={sm.timeline.title}></h2>
      <div class="sm-timeline">

        <div class="sm-tl-item reveal">
          <div class="sm-tl-dot own"></div>
          <div class="sm-tl-card own">
            <span class="sm-badge own">{sm.timeline.own.badge}</span>
            <h3>{sm.timeline.own.title}</h3>
            <p class="sm-tl-where">{sm.timeline.own.where}</p>
            <p>{sm.timeline.own.desc1}</p>
            <p style="margin-top:.6rem">{sm.timeline.own.desc2}</p>
            <div class="sm-tags"><span>Astro</span><span>Laravel</span><span>JavaScript</span><span>SEO</span><span>PHP</span><span>Full Stack</span></div>
          </div>
        </div>

        <div class="sm-tl-item reveal">
          <div class="sm-tl-dot edu now"></div>
          <div class="sm-tl-card now">
            <span class="sm-badge now">{sm.timeline.daw.badge}</span>
            <h3>{sm.timeline.daw.title}</h3>
            <p class="sm-tl-where">{sm.timeline.daw.org}</p>
            <p>{sm.timeline.daw.desc}</p>
            <div class="sm-tags"><span>PHP</span><span>JavaScript</span><span>MySQL</span><span>HTML/CSS</span></div>
          </div>
        </div>

        <div class="sm-tl-item reveal">
          <div class="sm-tl-dot edu"></div>
          <div class="sm-tl-card">
            <span class="sm-badge edu">{sm.timeline.ia.badge}</span>
            <h3>{sm.timeline.ia.title}</h3>
            <p class="sm-tl-where">{sm.timeline.ia.org}</p>
            <p>{sm.timeline.ia.desc}</p>
            <div class="sm-tags"><span>Python</span><span>Pandas</span><span>ML</span></div>
          </div>
        </div>

        <div class="sm-tl-item reveal">
          <div class="sm-tl-dot work"></div>
          <div class="sm-tl-card">
            <span class="sm-badge work">{sm.timeline.educa.badge}</span>
            <h3>{sm.timeline.educa.title}</h3>
            <p class="sm-tl-where">{sm.timeline.educa.org}</p>
            <p>{sm.timeline.educa.desc}</p>
            <div class="sm-tags"><span>Laravel</span><span>PHP</span><span>Google Cloud</span><span>ElasticSearch</span></div>
          </div>
        </div>

        <div class="sm-tl-item reveal">
          <div class="sm-tl-dot work"></div>
          <div class="sm-tl-card">
            <span class="sm-badge work">{sm.timeline.euroinnova.badge}</span>
            <h3>{sm.timeline.euroinnova.title}</h3>
            <p class="sm-tl-where">{sm.timeline.euroinnova.org}</p>
            <p>{sm.timeline.euroinnova.desc}</p>
            <div class="sm-tags"><span>Laravel</span><span>PHP</span><span>MySQL</span><span>JavaScript</span></div>
          </div>
        </div>

        <div class="sm-tl-item reveal">
          <div class="sm-tl-dot edu"></div>
          <div class="sm-tl-card">
            <span class="sm-badge edu">{sm.timeline.dam.badge}</span>
            <h3>{sm.timeline.dam.title}</h3>
            <p class="sm-tl-where">{sm.timeline.dam.org}</p>
            <p>{sm.timeline.dam.desc}</p>
            <div class="sm-tags"><span>Java</span><span>SQL</span><span>Android</span><span>Hibernate</span></div>
          </div>
        </div>

      </div>
    </section>

    <!-- Stack -->
    <section class="sm-section sm-stack-section">
      <h2 class="sm-title reveal" set:html={sm.stack.title}></h2>
      <div class="sm-pills">
        <span class="sm-pill sm-pill--high reveal" style="--d:0s">⚡ Laravel</span>
        <span class="sm-pill sm-pill--high reveal" style="--d:.05s">🐘 PHP</span>
        <span class="sm-pill sm-pill--high reveal" style="--d:.08s">🟨 JavaScript</span>
        <span class="sm-pill sm-pill--high reveal" style="--d:.11s">🗄️ MySQL</span>
        <span class="sm-pill sm-pill--high reveal" style="--d:.14s">🎨 HTML / CSS</span>
        <span class="sm-pill sm-pill--high reveal" style="--d:.17s">☕ Java</span>
        <span class="sm-pill sm-pill--mid reveal" style="--d:.20s">🐍 Python</span>
        <span class="sm-pill sm-pill--mid reveal" style="--d:.23s">☁️ Google Cloud</span>
        <span class="sm-pill sm-pill--mid reveal" style="--d:.26s">🔍 ElasticSearch</span>
        <span class="sm-pill sm-pill--low reveal" style="--d:.29s">🐙 Git</span>
        <span class="sm-pill sm-pill--low reveal" style="--d:.32s">🐳 Docker</span>
        <span class="sm-pill sm-pill--low reveal" style="--d:.35s">🚀 Astro</span>
      </div>
    </section>

    <!-- Hobbies -->
    <section class="sm-section">
      <h2 class="sm-title reveal" set:html={sm.hobbies.title}></h2>
      <p class="sm-subtitle reveal">{sm.hobbies.subtitle}</p>
      <div class="sm-hobbies">
        <div class="sm-hobby reveal" style="--d:0s">
          <div class="sm-hobby-ico">🎾</div>
          <h3>{sm.hobbies.padel.title}</h3>
          <p>{sm.hobbies.padel.desc}</p>
        </div>
        <div class="sm-hobby reveal" style="--d:.1s">
          <div class="sm-hobby-ico">🏋️</div>
          <h3>{sm.hobbies.gym.title}</h3>
          <p>{sm.hobbies.gym.desc}</p>
        </div>
        <div class="sm-hobby reveal" style="--d:.2s">
          <div class="sm-hobby-ico">🥾</div>
          <h3>{sm.hobbies.routes.title}</h3>
          <p>{sm.hobbies.routes.desc}</p>
        </div>
        <div class="sm-hobby reveal" style="--d:.3s">
          <div class="sm-hobby-ico">🌿</div>
          <h3>{sm.hobbies.bonsai.title}</h3>
          <p>{sm.hobbies.bonsai.desc}</p>
        </div>
        <div class="sm-hobby reveal" style="--d:.4s">
          <div class="sm-hobby-ico">📸</div>
          <h3>{sm.hobbies.photo.title}</h3>
          <p>{sm.hobbies.photo.desc}</p>
        </div>
        <div class="sm-hobby reveal" style="--d:.5s">
          <div class="sm-hobby-ico">✈️</div>
          <h3>{sm.hobbies.travel.title}</h3>
          <p>{sm.hobbies.travel.desc}</p>
        </div>
      </div>
    </section>

  </div>

  <script>
    const canvas = document.getElementById('sm-canvas');
    const ctx = canvas.getContext('2d');
    const mouse = { x: -9999, y: -9999 };
    const N = 70;
    let particles = [];

    function resize() {
      canvas.width  = window.innerWidth;
      canvas.height = window.innerHeight;
    }

    function initParticles() {
      particles = Array.from({ length: N }, () => ({
        x:  Math.random() * canvas.width,
        y:  Math.random() * canvas.height,
        vx: (Math.random() - .5) * .35,
        vy: (Math.random() - .5) * .35,
        r:  Math.random() * 1.8 + .8,
      }));
    }

    function drawFrame() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      for (let i = 0; i < particles.length; i++) {
        const p = particles[i];
        const dx = p.x - mouse.x, dy = p.y - mouse.y;
        const dist = Math.hypot(dx, dy);
        if (dist < 110 && dist > 0) {
          const f = (110 - dist) / 110;
          p.vx += (dx / dist) * f * .6;
          p.vy += (dy / dist) * f * .6;
        }
        p.vx *= .98; p.vy *= .98;
        const spd = Math.hypot(p.vx, p.vy);
        if (spd > 1.8) { p.vx = p.vx / spd * 1.8; p.vy = p.vy / spd * 1.8; }
        p.x += p.vx; p.y += p.vy;
        if (p.x < 0) p.x = canvas.width;
        if (p.x > canvas.width) p.x = 0;
        if (p.y < 0) p.y = canvas.height;
        if (p.y > canvas.height) p.y = 0;
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
        ctx.fillStyle = 'rgba(42,82,152,.75)';
        ctx.fill();
        for (let j = i + 1; j < particles.length; j++) {
          const q = particles[j];
          const d = Math.hypot(p.x - q.x, p.y - q.y);
          if (d < 130) {
            ctx.beginPath(); ctx.moveTo(p.x, p.y); ctx.lineTo(q.x, q.y);
            ctx.strokeStyle = `rgba(42,82,152,${(1 - d / 130) * .25})`;
            ctx.lineWidth = 1; ctx.stroke();
          }
        }
      }
      requestAnimationFrame(drawFrame);
    }

    window.addEventListener('mousemove', e => { mouse.x = e.clientX; mouse.y = e.clientY; });
    window.addEventListener('resize', () => { resize(); initParticles(); });

    document.querySelector('.sm-scroll-hint').addEventListener('click', e => {
      e.preventDefault();
      const target = document.getElementById('stats');
      if (!target) return;
      const top = target.getBoundingClientRect().top + window.scrollY - 80;
      window.scrollTo({ top, behavior: 'smooth' });
    });

    resize(); initParticles(); drawFrame();

    const revealObs = new IntersectionObserver(entries => {
      entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('revealed'); revealObs.unobserve(e.target); } });
    }, { threshold: .12 });
    document.querySelectorAll('.reveal, .ani').forEach(el => revealObs.observe(el));

    const counterObs = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;
        const el = entry.target, target = parseInt(el.dataset.target);
        let current = 0;
        const stepVal = target / 28;
        const id = setInterval(() => {
          current += stepVal;
          if (current >= target) { current = target; clearInterval(id); }
          el.textContent = Math.floor(current);
        }, 45);
        counterObs.unobserve(el);
      });
    }, { threshold: .5 });
    document.querySelectorAll('.counter').forEach(el => counterObs.observe(el));
  </script>

</Layout>
